# 2026-09-09

## 部署迁移：Netlify(提交 public/) → GitHub Pages（Actions 直接部署）

- 背景确认：原流程 `export-weread-notes.yml` 每天/每次 push：导数据 → Hugo 构建 → **把 public/ 提交回 main** → Netlify 直接托管仓库里的 public/。运行全部成功，用户的诉求是让 Actions 构建后直接上传部署，不再提交构建产物。
- 用户选择：迁移到 GitHub Pages（自定义域名 blog.zzek.cn 保留）。
- 改动：
  1. 重写 `.github/workflows/export-weread-notes.yml`：permissions 增加 pages:write + id-token:write；concurrency 组改 "pages"；Hugo 版本从 latest 固定为 0.165.0（与本地一致，防模板行为漂移）；新增 actions/configure-pages@v5（enablement:true，continue-on-error）→ `hugo --gc --minify` → 机器人**只提交数据与内容**（static/data/*.json、content/weread/、content/memos/，不再 add public/）→ actions/upload-pages-artifact@v4（path ./public）→ actions/deploy-pages@v4。机器人提交仍带 [skip ci] 防循环。
  2. 新增 `static/CNAME`（内容 blog.zzek.cn），随构建进入产物根目录，让 GitHub Pages 记住自定义域。
- 用户侧切换步骤（告知）：① push 后确认 Actions 首次部署成功（若 Pages Source 未自动设为 GitHub Actions，去 Settings→Pages 手动设）；② DNS 把 blog CNAME 从 Netlify 值改为 `ilikui.github.io`，等证书签发后开 Enforce HTTPS；③ 切换稳定后清理：`git rm -r --cached public` + `.gitignore` 加 `public/`（并更新其中关于 Netlify 的注释）+ 推送，然后解绑/删除 Netlify 站点。
- 注意：在步骤③之前不要把 public/ 移出 git——Netlify 还在托管提交的 public/，过早删除会导致旧域名立即 404。
- 验证：workflow YAML 语法通过 pyyaml 校验；本地 `hugo --quiet` 构建通过且 public/CNAME 存在。

## 热力图贪吃蛇动效（2026-09-09 晚）

- `layouts/activity.html`：新增贪吃蛇动画——蛇头按蛇形路径（偶数周列自上而下、奇数列自下而上）爬过热力格子，活跃格被"吃掉"（`s-eaten` 持续发亮+咬合弹出），空格留渐隐轨迹（`s-trail`），爬完停留 2.6s 后自动循环；工具栏加"🐍 重播"按钮；切换类型芯片会重启动画；`prefers-reduced-motion` 下整体禁用。
- `assets/css/weread-theme.css`：追加 `.hm-snake-layer/.hm-snake-head/s-eaten/s-trail` 样式（绿色渐变蛇头、hmBite/hmTrail 动画），`.hm-body` 加 `position:relative`。
- 实现细节：蛇头为绝对定位元素叠在 `.hm-body` 上（x 偏移 22px = 周标签 16px + 间距 6px，格距 14px），JS setInterval 22ms/步驱动。
- 本机 1313 端口被 Windows 保留段拒绝（bind 报错），预览服务器改用 8131 端口。

### 升级为 Platane/snk 同款（同日稍晚）

- 参考 `github.com/ilikui/home` 的 contribution-grid-snake.svg，把蛇从"单头+轨迹"升级为带身体的蛇：头 1 节 + 身体 12 节（`.hm-snake-head`/`.hm-snake-seg`，紫色系 #a371f7 头 / #8250df 身，尾段渐淡），每步 26ms，身体沿 history FIFO 依次跟随。
- 被吃的活跃格闪绿光（hmBite 弹出+绿色光晕），空格不再留轨迹。
- 修正 bug：上一版 `.hm-snake-layer` 图层 div 漏插进 HTML，`$('hmSnakeLayer')` 为 null 导致动画实际未运行；现已补上。
- 顺带清掉了旧的 s-trail 渐隐轨迹样式。

### 年份切换 + 当日卡片 + 蛇减速（同日 20:20）

- 热力墙页大改（layouts/activity.html 整体重写）：
  - 年份切换：工具栏新增 #hmYears 芯片（近一年/2026/2025/2024/2023，由全部数据推导），applyRange() 按 recent 或具体年份计算 start/end/weeks，历史年份窗口到 12-31。
  - 点击格子弹出当日卡片（#hmPop，绝对定位在 .hm-card 内自适应位置）：显示当天阅读划线/想法（带书名）、博客（带链接）、Memo（带 flomo 链接），受类型筛选联动，Esc/点外部关闭。
  - 数据从纯时间戳升级为条目对象（t/b/h/c、title/url），在 Hugo 构建期内联；memo 标题用全局 replace 清理 Markdown 星号（strings.ReplaceAll 在 Hugo 0.165 不可用）。
  - 蛇速 STEP_MS 26→48（全程约 18 秒）。
- CSS 追加 .hm-pop 弹层样式；.hm-card 加 position:relative。
- 验证：构建 0 错误，/activity/ 200，四年份条目 2023:9 / 2024:392 / 2025:12 / 2026:144。

### 近一年也按年份字形走（同日 20:37）

- 按用户要求：snakePath() 不再区分模式，始终走年份数字字形；"近一年"用当前年份（new Date().getFullYear()），具体年份芯片用所选年份。snakeVisit 恒定点亮走过的格子；蛇形（serpentine）路径仅保留为字形放不下时的兜底。
