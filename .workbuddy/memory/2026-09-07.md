# 2026-09-07

## 新增「热力墙」Activity 页面（/activity/）

- 用户需求：综合 阅读(微信读书)、博客、Memos 三类内容的活动热力图（GitHub 风格贡献格子），独立页面并加入导航（顶栏「热力墙」+ 侧栏 Activity）。
- 新增文件：
  - `content/activity/_index.md`（title 热力墙，layout: activity）
  - `layouts/activity.html` —— 独立 weread-page 布局页
  - CSS：热力墙组件样式追加到 weread-theme.css 末尾；`:root` 与 dark 块新增 `--hm-0..4` 五档色阶变量
  - hugo.yml：side 菜单加 Activity(/activity/)，main 菜单加 热力墙(/activity/)
- 数据聚合在**构建期**完成（静态无请求）：
  - `readFile "static/data/weread_notes.json"` + `transform.Unmarshal`，取出全部 notes.created_at（Unix 秒，模板里 `printf "%.0f"` 转字符串防 float64 科学计数法）
  - memos 的 ISO 时间用 `time.AsTime .created_at).Unix` 转秒
  - 博客用 `site.RegularPages blog` 的 `.Date.Unix`
  - 三组秒级时间戳 `jsonify` 内联进页面 JS；注意 `<script>` 上下文里 jsonify 输出会被 Go 模板再转义成字符串，必须 `| safeJS`
- 前端渲染：近 365 天、周一开头、列=周；月份标签 + 一/三/五 行标；四档色阶按活跃度四等分；悬浮 title 显示当日三类计数；芯片切换 全部/阅读/博客/Memos；4 张统计卡（阅读活跃天/博客篇数/Memos 条数/活跃天数）；侧栏累计统计 + 说明；窄屏横滑（hm-scroll）。
- 验证：node 脚本按与页面相同的分桶逻辑核对 → 494 笔记/5 博客/7 memo，窗口内阅读活跃 9 天、博客 3、memos 5、总活跃 14 天，合理。
- 对齐细节：周标签列宽 16px + flex gap 6px → 月份行 margin-left 应为 22px；`.hm-cell` 需 display:inline-block 供图例使用（grid 内会被 blockify，无影响）。
